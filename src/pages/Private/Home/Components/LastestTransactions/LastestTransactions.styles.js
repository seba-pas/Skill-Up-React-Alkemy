import styled from 'styled-components';

export const ContentLastestTransactions = styled.div`
    margin: 40px 0 0 0;

    .top {
        margin: 0 auto 20px;
        max-width: 450px;
        a .label {
            font-size: 1.2rem;
        }
    }
`;
